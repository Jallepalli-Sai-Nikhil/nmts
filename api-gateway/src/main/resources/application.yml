spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
        max-attempts: 5
  config:
    import: "optional:configserver:http://localhost:8888"

# The following local properties will be overridden by Config Server if successfully loaded
server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

nmts:
  jwt:
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
