server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nmts_report?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
